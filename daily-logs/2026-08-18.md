# 2026-08-18

## Goals

- Double-check this repo's contents to see if it's safe to post publically
- Continue studying MultiUAV-Plat READMEs
- Figure out and import an example session.
- Run one simple manual drone task.
- Export the session/result and save it in the experiments folder.

## Today's Work

- Adjusted experiments folder format and updated README
- Added `.gitignore` coverage for sample session starter files
- Attempted example session launch
- Clicking "View Full Data" returned blank window and popup with "Failed to load session data"
- Switched controller work toward conda environment instead of the old `.venv`
- Added the missing `shapely` dependency to controller requirements
- Launched the provided example session, exported json file to experiments folder
- Attempted to make a task from template, returned "Request timed out" popup
- Running controller additionally returns "request timed out" popup after 30 seconds
- Tried to debug by removing VPN routing, still timed out

## Results

- Server and controller can both start, but controller loads after 30 seconds with timeout popup
- Launched provided example session
- Found that normal session `GET` requests work quickly
- Found that task-related `POST` requests time out, including collision checks and task creation
- Codex identified likely cause as server request-logging middleware reading the POST body before the FastAPI route handles it
- Did not complete a manual task because task creation is currently blocked by the timeout issue

## Notes

- The controller's requirements.txt lacks the same `shapely` dependency as the server (an oversight?)
- Current blocker is the controller task creation timeout
- Starting controller returns same timeout popup

## Next Steps

- Continue debugging and find patch for controller timeout issue
- Run one simple manual drone task
- Configure Agent4Drone with API key (likely doubao)
- Export the example session/result and save it in the experiments folder with screenshots and notes
